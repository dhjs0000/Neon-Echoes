# Neon Echoes 谱面格式文档

## 文件格式

谱面文件使用 `.chart` 扩展名，采用纯文本格式，编码为 UTF-8。

## 基本结构

谱面文件由以下部分组成：
1. **元数据区** - 定义谱面的基本信息
2. **时间控制区** - 设置音符出现的时间点
3. **音符定义区** - 定义具体的音符元素
4. **结束标记** - 标记谱面结束

---

## 元数据元素

元数据位于文件开头，用于定义谱面的基本信息。

### 1. name - 曲目名称

**格式：** `name-<曲目名称>`

**作用：** 定义谱面的显示名称。

**示例：**
```
name-RUSH E
name-死别
```

---

### 2. maker - 作者信息

**格式：** `maker-<谱面作者>-<歌曲作者>`

**作用：** 
- 定义谱面制作者
- 可选：定义原曲作者

**示例：**
```
maker-Unknown
maker-TRG-ABC
```

---

### 3. level - 难度等级

**格式：** `level-<等级数字>-<难度名称>`

**作用：** 定义谱面的难度等级和难度分类。

**难度名称：**
- `EZ` - Easy（简单）
- `HD` - Hard（困难）
- `IN` - Insane（疯狂）(存疑?)
- `AT` - Another（额外）(存疑?)
- `SP` - Special（特殊）(存疑?)

**示例：**
```
level-5-HD    # HD难度，等级5
level-1-EZ    # EZ难度，等级1
level-?-SP    # SP难度，无数字等级
```

---

### 4. audio - 音频文件

**格式：** `audio-<音频文件名>`

**作用：** 指定谱面对应的音频文件。

**特殊值：**
- `audio-N` - 表示无音频文件

**示例：**
```
audio-Rush E.mp3
audio-死别.mp3
audio-N         # 无音频
```

**注意：** 音频文件应放在 `audio/` 目录下。

---

### 5. speed - 音符流速

**格式：** `speed-<速度值>` 或 `line-<速度值>`

**作用：** 定义音符下落的速度（行/秒）。

**默认值：** 5.0

**示例：**
```
speed-10.0    # 每秒10行
speed-5       # 每秒5行
line-8.5      # 使用line-前缀也可以
```

**说明：**
- 数值越大，音符下落越快
- 影响 HOLD 音符的长度计算

---

## 时间控制元素

### 时间戳格式

**格式：** `<分>:<秒>` 或 `<分>:<秒>:<毫秒>`

**作用：** 设置当前时间基准，后续音符将以此时间为参考。

**示例：**
```
0:0:706       # 0分0秒706毫秒
0:12:252      # 0分12秒252毫秒
1:30          # 1分30秒0毫秒
```

**重要说明：**
- 时间戳必须单独一行
- 时间戳之后的所有音符都使用此时间
- 时间戳可以多次出现，用于精确控制音符时机

---

## 音符元素

### 1. tab - 普通音符

**格式：** `tab-<轨道编号>`

**作用：** 在指定轨道创建一个普通点击音符。

**轨道编号：** 1-10（对应游戏中的10个轨道）

**示例：**
```
0:0:706
tab-5         # 在轨道5创建普通音符
0:3:893
tab-6         # 在轨道6创建普通音符
```

**显示：** 绿色方块 `######`

---

### 2. hold - 长按音符

**格式：** `hold-<轨道编号>-<长度>`

**作用：** 在指定轨道创建一个需要长按的音符。

**参数：**
- `轨道编号`：1-10
- `长度`：音符持续的行数（可选，默认1行）

**示例：**
```
0:01:590
tab-3
hold-5-30     # 在轨道5创建长按音符，持续30行
```

**显示：** 蓝色长条 `██████`，带渐变效果

**判定机制：**
- 音符头部到达判定线时按下对应轨道键
- 保持按住直到音符尾部通过判定线
- 提前松开会获得部分判定

---

### 3. drag - 拖动音符

**格式：** `drag-<轨道编号>`

**作用：** 在指定轨道创建一个拖动音符。

**轨道编号：** 1-10

**示例：**
```
0:03:260
drag-2        # 在轨道2创建拖动音符
0:03:598
drag-3        # 在轨道3创建拖动音符
```

**显示：** 黄色符号 `$$$$$$`

**判定机制：**
- 音符到达判定线时，对应轨道必须处于按下状态
- 与普通音符类似，但需要保持轨道激活

---

## 文字事件元素

### write - 屏幕文字

**格式：** `write-<文字内容>-<显示秒数>`

**作用：** 在屏幕上显示文字信息。

**参数：**
- `文字内容`：要显示的文字（可以包含 `-`）
- `显示秒数`：文字显示的持续时间（秒）

**示例：**
```
0:10:000
write-准备好了吗-3    # 显示"准备好了吗"，持续3秒
write-Start!-2        # 显示"Start!"，持续2秒
```

---

## 结束标记

### & - 谱面结束

**格式：** `&`

**作用：** 标记谱面结束位置。

**示例：**
```
0:07
&             # 谱面在此处结束
```

**说明：**
- 结束标记后的内容将被忽略
- 用于精确控制歌曲结束时机

---

## 完整示例

```
name-示例谱面
maker-测试作者
level-3-HD
audio-example.mp3
speed-8.0

# 开始部分
0:0:000
tab-5
write-准备好了吗-2

0:2:000
tab-4
tab-6

0:4:000
hold-5-20     # 长按音符

0:6:000
drag-3
drag-7

# 结束
2:00:000
&
```

---

## 注意事项

1. **轨道编号**：所有轨道编号从1开始（1-10），在内部会转换为0-9

2. **时间精度**：毫秒级精度，建议使用 `分:秒:毫秒` 格式获得最佳效果

3. **音符排序**：音符按时间戳自动排序，不需要手动排序

4. **空行和注释**：
   - 空行会被忽略
   - 以 `#` 开头的行是注释

5. **文件编码**：必须使用 UTF-8 编码，支持中文

6. **音频同步**：确保音频文件与谱面时间戳同步

7. **流速影响**：
   - HOLD 音符的实际持续时间 = 长度 / speed
   - speed 值越大，HOLD 音符显示越短

---

## 文件位置

谱面文件应放在 `charts/` 目录下，音频文件放在 `audio/` 目录下。

```
项目目录/
├── charts/
│   ├── rush e.chart
│   ├── 死别.chart
│   └── 自定义谱面.chart
├── audio/
│   ├── Rush E.mp3
│   ├── 死别.mp3
│   └── example.mp3
└── ...
```
